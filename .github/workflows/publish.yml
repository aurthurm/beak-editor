name: Publish to GitHub Packages

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v2
        with:
          version: 8

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@amusendame'

      - name: Extract version from tag
        id: version
        run: |
          # Remove 'v' prefix from tag (v1.0.0-beta2 -> 1.0.0-beta2)
          VERSION=${GITHUB_REF_NAME#v}
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Publishing version: $VERSION"

      - name: Update package versions
        run: |
          VERSION=${{ steps.version.outputs.version }}
          # Update core package version
          cd packages/core
          npm pkg set version="$VERSION"
          cd ../react
          # Update react package version
          npm pkg set version="$VERSION"
          # Update dependency on core to use exact version
          npm pkg set dependencies.@amusendame/beakblock-core="$VERSION"

      - run: pnpm install --no-frozen-lockfile

      - name: Build packages
        run: |
          pnpm --filter @amusendame/beakblock-core build
          pnpm --filter @amusendame/beakblock-react build

      - name: Publish @amusendame/beakblock-core
        run: pnpm --filter @amusendame/beakblock-core publish --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish @amusendame/beakblock-react
        run: pnpm --filter @amusendame/beakblock-react publish --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
