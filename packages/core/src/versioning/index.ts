/**
 * Document versioning (pluggable snapshots and restore).
 *
 * @module
 */

export type { DocumentVersion, VersioningAdapter } from './types';
export { InMemoryVersioningAdapter } from './inMemoryAdapter';
